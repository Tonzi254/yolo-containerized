apiVersion: apps/v1
kind: Deployment
metadata:
  name: k8s-backend
spec:
  selector:
    matchLabels:
      app: yolo
      tier: backend
      track: stable
  replicas: 1
  template:
    metadata:
      labels:
        app: yolo
        tier: backend
        track: stable
  spec:
    containers:
      - name: yolo-backend
        image: tonzi/yolo-backend:1.0.0
        ports:
          - containerPort: 5000
